# =============================================================================
# Costa Rica Tree Atlas - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in values as needed.
# Most variables are optional - the site works without any API keys.
# =============================================================================

# =============================================================================
# Admin Authentication (REQUIRED for /admin routes)
# =============================================================================

# Admin username (defaults to "admin" if not set)
# Tip: Use a non-obvious username for better security
ADMIN_USERNAME=admin

# Admin password (REQUIRED - use a strong password!)
# Generate with: openssl rand -base64 32
# Without this, admin routes will return 503 Service Unavailable
ADMIN_PASSWORD=

# =============================================================================
# Rate Limiting (Optional - for production)
# =============================================================================

# Upstash Redis for persistent rate limiting across server restarts
# Sign up at https://upstash.com
# Without these, the app uses in-memory rate limiting (resets on restart)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Analytics (optional - choose one or none)
# -----------------------------------------------------------------------------

# Plausible Analytics (privacy-first, no cookies, recommended)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Simple Analytics (privacy-first, GDPR compliant)
NEXT_PUBLIC_ENABLE_SIMPLE_ANALYTICS=false

# Google Analytics (GA4) - requires cookie consent banner
NEXT_PUBLIC_GA_ID=

# -----------------------------------------------------------------------------
# Features Currently Disabled
# -----------------------------------------------------------------------------
# These features are disabled in the codebase but kept here for future use.

# Google Cloud Vision API (identify feature - currently disabled)
# GOOGLE_CLOUD_VISION_API_KEY=

# Google Maps API (field trip map - currently shows fallback)
# NEXT_PUBLIC_MAPS_API_KEY=

# =============================================================================
# Rate Limiting (REQUIRED for production)
# =============================================================================

# Upstash Redis for persistent rate limiting across serverless functions
# Sign up for free at https://upstash.com
# Create a Redis database and copy the REST URL and token
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here
